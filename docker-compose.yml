services:
  moon:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: moon-app
    ports:
      - "8888:8888"
    volumes:
      - ./backend/data:/app/backend/data
    restart: unless-stopped
